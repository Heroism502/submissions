# Model checkpoints

| File | In git? | How to obtain |
|------|---------|---------------|
| `DenoiseFlow-light-UVG-finetune.ckpt` | Yes (~3 MB) | Bundled |
| `kitti360_com.pth` | No (>100 MB GitHub limit) | `bash src/download_pretrained.sh` (SuperPC Google Drive Model Zoo) |

Both are required before `bash src/run_smoke.sh`.
